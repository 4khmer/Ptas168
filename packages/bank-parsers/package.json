{
  "name": "@ptas/bank-parsers",
  "version": "0.0.0",
  "private": true,
  "description": "Pure parsers for bank payment-confirmation text (ABA, ACLEDA, …). No deps, no I/O — both apps/backend and apps/telegram-bot consume.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch --preserveWatchOutput",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
