{
  "name": "ptas168",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:backend": "pnpm --filter ptas168-backend dev",
    "dev:frontend": "pnpm --filter pbms-app dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "db:up": "docker compose -f infrastructure/docker/docker-compose.yml up -d",
    "db:down": "docker compose -f infrastructure/docker/docker-compose.yml down",
    "db:logs": "docker compose -f infrastructure/docker/docker-compose.yml logs -f",
    "db:reset": "docker compose -f infrastructure/docker/docker-compose.yml down -v && docker compose -f infrastructure/docker/docker-compose.yml up -d",
    "db:psql": "docker compose -f infrastructure/docker/docker-compose.yml exec postgres psql -U postgres -d ptas168_dev",
    "db:generate": "pnpm --filter @ptas/db generate",
    "db:migrate": "pnpm --filter @ptas/db migrate",
    "db:deploy": "pnpm --filter @ptas/db deploy",
    "db:studio": "pnpm --filter @ptas/db studio",
    "db:seed": "pnpm --filter @ptas/db seed",
    "db:demo-seed": "pnpm --filter @ptas/db demo-seed"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
